# 1. Install the new package
cd "C:\Camera App\camera-ocr-app"
npm install react-native-view-shot --legacy-peer-deps

# 2. Rebuild native project (new native dependency requires it)
npx expo prebuild --clean
npx expo run:android

# 3. Start Metro
npx expo start --dev-client --port 3030
