# 1. Kill all Java processes holding the lock
Stop-Process -Name "java" -Force -ErrorAction SilentlyContinue

# 2. Delete the android folder
Remove-Item -Path "C:\Camera App\android" -Recurse -Force

# 3. Retry prebuild
cd "C:\Camera App\camera-ocr-app"
npx expo prebuild --clean
