
# 1. Kill all Gradle daemons
cd "C:\Camera App\camera-ocr-app"
gradlew --stop 2>nul

# 2. Kill any lingering Java processes holding the lock
taskkill /F /IM java.exe 2>nul

# 3. Now delete the android folder manually
rmdir /s /q "C:\Camera App\android"

# 4. Retry prebuild
npx expo prebuild --clean
