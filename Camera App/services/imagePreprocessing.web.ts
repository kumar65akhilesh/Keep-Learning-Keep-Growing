/**
 * Image Preprocessing — Web stub.
 * Returns the URI unchanged since native manipulations aren't available.
 */

export async function preprocessImage(uri: string): Promise<string> {
  return uri;
}

export async function cropRegion(uri: string): Promise<string> {
  return uri;
}
